public class CodecFactory {
    public Codec extract(VideoFile file) {
        // Implementation logic to extract codec based on the video file
        // For simplicity, returning a dummy codec here
        return new Codec();
    }
}
